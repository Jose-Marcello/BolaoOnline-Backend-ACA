trigger:
  - main

pool:
  vmImage: "windows-latest"

stages:
  - stage: BuildFrontend
    displayName: "Build do Frontend (Diagnóstico)"
    jobs:
      - job: FrontendJob
        displayName: "Job de Build do Frontend"
        steps:
          # 1. Instala o Node.js
          - task: NodeTool@0
            displayName: "Instalar Node.js"
            inputs:
              versionSpec: "18.x"

          # 2. Compila o Frontend (Angular)
          - script: |
              cd $(Build.SourcesDirectory)/src/