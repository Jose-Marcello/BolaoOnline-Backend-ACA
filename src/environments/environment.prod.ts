// Localização: src/environments/environment.prod.ts
export const environment = {
  production: true,
  apiUrl: 'http://localhost:5288/', // Sua URL base da API de produção
  imagesUrl: '/assets/images/' // <<-- NOVO: URL base para imagens estáticas do frontend -->>
};
