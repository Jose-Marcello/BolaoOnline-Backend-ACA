
@* @using Microsoft.AspNetCore.Identity
@using ApostasApp.Core.Domain.Models.Usuarios

@inject SignInManager<Usuario> SignInManager

<ul class="navbar-nav mr-auto">

    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <form method="post" asp-controller="account" asp-action="Logout">
                <button type="submit" class="nav-link btn btn-secondary py-0">
                    Logout @User.Identity.Name
                </button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link btn btn-secondary" asp-controller="account" asp-action="register">
                Registrar
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link btn btn-secondary" asp-controller="account" asp-action="login">
                Login
            </a>
        </li>
    }

</ul> *@